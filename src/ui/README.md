# Tầng PRESENTATION — component dùng chung

Đây là chỗ của nhánh 2 trong WBS: AppHeader, BottomTabBar, SearchBox, NumberInput,
LinkedInput, ResultBlock, ErrorState… Mỗi component một thư mục.

Ràng buộc CON-03: không được import thẳng `@/core/*` hay `@/data/*`.
Mọi thứ đi qua `@/application`.
