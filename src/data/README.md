# Tầng DATA

Nơi ở của `DataProvider` và các bộ số liệu mẫu tĩnh (FPT, HPG…) mà wireframe WF-10 vẽ.

Chưa có gì ở đây vì đang chờ Finbox cấp dữ liệu mẫu — xem giả định A1 và rủi ro R-01 trong SRS.

Ràng buộc: không được import React hay Next, không được gọi lên tầng giao diện.
